// Exact paths listed here render the 404 page instead of whatever route they
// would otherwise match — use this to retire/disable a specific route without
// deleting its page component. Add paths as plain strings, e.g. '/old-demo'.
export const BLOCKED_ROUTES = [];
